management:
  endpoints:
    web:
      exposure:
        include: '*'
spring:
  datasource:
    password: senha
    url: jdbc:postgresql://localhost:5432/san-admin
    username: msansone
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQL9Dialect
    hibernate:
      ddl-auto: update
    show-sql: true

  security:
    user:
      name: admin
      password: batata